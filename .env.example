# KaosNet Environment Configuration
# Copy this file to .env and customize for your environment
#
# Quick Start:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# =============================================================================
# REQUIRED - Set these before running!
# =============================================================================

# JWT secret for authentication (min 32 characters)
# Generate with: openssl rand -base64 32
KAOS_JWT_SECRET=change-me-generate-with-openssl-rand-base64-32

# Admin account password
KAOS_ADMIN_PASSWORD=change-me-use-a-strong-password

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Environment: development, staging, production
KAOS_ENV=development

# Admin username
KAOS_ADMIN_USERNAME=admin

# Log level: trace, debug, info, warn, error
RUST_LOG=info,kaosnet=debug

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=kaos
POSTGRES_PASSWORD=kaos
POSTGRES_DB=kaosnet
POSTGRES_PORT=5432

# =============================================================================
# OPTIONAL FEATURES
# =============================================================================

# Enable metrics endpoint (/metrics)
# KAOS_METRICS_ENABLED=true

# Rate limiting (requests per second)
# KAOS_RATE_LIMIT=100

# =============================================================================
# CONSOLE UI
# =============================================================================

# API URL for the console frontend
VITE_API_URL=http://localhost:7350
